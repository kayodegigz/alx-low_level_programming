#include <stdio.h>

/**
*main - Prints out all hexadecimal numbers
*
* Return: Returns 0 when successfully ran
*/

int main(void)
{
	int n;
	char c;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch)
	}
	putchar('\n');
	return (0);
}
