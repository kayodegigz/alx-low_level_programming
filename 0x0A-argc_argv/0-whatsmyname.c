#include <stdio.h>
/**
 * main - the main function
 *
 * @argc: the number of arguments in the array name
 * @argv: the one dimensional array of the arguments
 * Return: returns an integer value
 */
int main(int argc, char *argv)
{
    printf("%s",argv[0]);
    return (0);
}
